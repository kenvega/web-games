import type { SymbolMatchPrintedLayoutOverrides } from "./types.js";

// Reviewed card-specific replacements belong here. The generator applies these
// after deterministic generation so regenerating artifacts cannot erase them.
export const SYMBOL_MATCH_PRINTED_LAYOUT_OVERRIDES =
  {} satisfies SymbolMatchPrintedLayoutOverrides;
